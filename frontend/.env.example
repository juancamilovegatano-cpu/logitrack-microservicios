# Direcciones de los microservicios que consulta el panel.
# Maintenance pasó del 8002 al 8006: el 8002 lo ocupa tracking-service
# desde que el sistema corre completo.
# Vite solo expone al navegador las variables que empiezan por VITE_.
VITE_FLEET_URL=http://localhost:8001
VITE_MAINT_URL=http://localhost:8006
